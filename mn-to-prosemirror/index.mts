import { makeSchema } from './schema.mjs';

interface XMLParseOptions {
  documentTag: string;
}

interface ParseOptions {
  xml: XMLParseOptions;
}

export function parseDocument(doc: Document, options: ParseOptions) {
  f
}
